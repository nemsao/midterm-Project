import { NextFunction, Request, Response } from "express";


const checkTimeInput = (
 req:   Request,
 res: Response,
 next:NextFunction
) => {
  try {
    const {}=req.body
  }catch(err){

  }
};


export { };
